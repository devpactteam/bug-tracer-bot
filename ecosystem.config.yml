apps:
  - name: bug-tracer-queue
    cwd: .
    script: artisan
    interpreter: php8.3
    args: >-
      queue:work
      --queue=default
      --sleep=3
      --tries=3
      --timeout=120
      --max-time=3600
    instances: 1
    exec_mode: fork
    autorestart: true
    watch: false
